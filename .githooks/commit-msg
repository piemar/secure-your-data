#!/bin/sh
# Strip Co-authored-by trailers so only your name appears as author on GitHub.
# Removes lines that add Cursor, Lovable, or other bots as co-authors.
if [ ! -f "$1" ]; then exit 0; fi
tmp=$(mktemp)
grep -v -E '^[[:space:]]*Co-authored-by:' "$1" > "$tmp" && mv "$tmp" "$1"
exit 0
