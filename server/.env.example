MONGODB_URI=mongodb+srv://sa-enablement:auA86K9leO3H8Jlg@cluster0.tcrpd.mongodb.net/?appName=Cluster0 
MONGODB_DB_NAME=mongodb_mayhem
JWT_SECRET=change-me-to-a-long-random-string
PORT=3001
CORS_ORIGIN=http://localhost:8080
FULL_IDE_ENABLED=false
FULL_IDE_BASE_URL=http://localhost:13337
# Optional absolute API origin for IDE workspaceUrl (default: relative /ide/...)
# API_PUBLIC_URL=https://api.example.com
AWS_REGION=eu-north-1
# Optional explicit URI used by terminal-shell sessions for mongosh.
# If omitted, terminal sessions fall back to MONGODB_CONNECTION_STRING or MONGODB_URI.
# MONGOSH_CONNECTION_STRING=mongodb://localhost:27017

# Rate limiting (production-safe defaults; localhost/dev can bypass by default).
# API_RATE_LIMIT_WINDOW_MS=900000
# API_RATE_LIMIT_MAX=100
# AUTH_RATE_LIMIT_WINDOW_MS=900000
# AUTH_RATE_LIMIT_MAX=20
# RATE_LIMIT_BYPASS_LOCALHOST=true
# RATE_LIMIT_BYPASS_DEV=true
